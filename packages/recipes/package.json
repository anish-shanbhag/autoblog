{
  "name": "recipes",
  "version": "0.1.0",
  "license": "MIT",
  "author": "Anish Shanbhag",
  "main": "dist/index.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rimraf dist/**/* && tsc -p tsconfig.build.json && node esbuild.js",
    "lint": "eslint . --fix"
  },
  "engines": {
    "node": ">=12"
  },
  "devDependencies": {
    "config": "workspace:*",
    "esbuild": "^0.14.51",
    "eslint": "^8.19.0",
    "rimraf": "^3.0.2",
    "tsconfig": "workspace:*",
    "tslib": "^2.4.0",
    "typescript": "^4.7.4"
  }
}
