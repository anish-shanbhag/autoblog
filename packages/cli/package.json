{
  "name": "cli",
  "version": "0.1.0",
  "license": "MIT",
  "author": "Anish Shanbhag",
  "main": "dist/index.js",
  "bin": {
    "scaffold": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --noEmit && esbuild src/index.ts --bundle --minify --platform=node --sourcemap --outfile=dist/index.js --external:esbuild",
    "lint": "eslint . --fix"
  },
  "engines": {
    "node": ">=12"
  },
  "devDependencies": {
    "config": "workspace:*",
    "eslint": "^8.19.0",
    "tsconfig": "workspace:*",
    "tslib": "^2.4.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "chalk": "^5.0.1",
    "commander": "^9.4.0",
    "esbuild": "^0.14.51",
    "ora": "^6.1.2",
    "update-notifier": "^6.0.2"
  }
}
